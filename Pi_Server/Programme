RPi4 global: 2a02:3102:6894:5200:3e04:6d1d:4553:25c8
RPi4 Domain: lbraun.nl

pm2 => auto start

    pm2 start my_script.py --interpreter python3
    pm2 startup # Einfügen der neuen Programme in Startup Skript
    pm2 save # Speichern des Skripts

nginx 
    sudo systemctl enable nginx
    sudo systemctl start nginx

    http://[2a02:3102:6894:5200:3e04:6d1d:4553:25c8]

ufw => Firewall

    - Fire wall ist automatisch aktiviert und bootet mit dem System

    sudo apt install ufw -y
    sudo ufw default deny incoming  # Alle eingehenden Verbindungen blockieren
    sudo ufw default allow outgoing # Alle ausgehenden Verbindungen erlauben

    sudo ufw allow 22/tcp   # SSH
    sudo ufw allow 80/tcp   # HTTP
    sudo ufw allow 443/tcp  # HTTPS
    sudo ufw allow 5900     # VNC
    /etc/ufw/ufw.conf

    in der Datei IPv6 aktivieren

    IPV6=yes        # IPv6 aktivieren

    sudo ufw enable             # Firewall aktivieren
    sudo ufw status verbose    # Status der Firewall





✅ Möglichkeit 2: IPv4-Zugang über Tunnel oder Drittanbieter
Wenn du nur IPv6 hast, aber öffentlich über IPv4 erreichbar sein willst, gibt es Workarounds:

🔁 Option A: Reverse Proxy mit Cloudflare (kostenlos)
Cloudflare bietet dir A-Record über IPv4

Du verbindest dich dort → Cloudflare leitet an deine IPv6 weiter

Vorteil: kein eigener IPv4 nötig

👉 Vorgehen:

Erstelle einen kostenlosen Account bei cloudflare.com

Verwalte deine Domain dort

Setze einen A-Record mit beliebiger IPv4 (Cloudflare übernimmt das)

Leite den Verkehr intern an deine IPv6-Adresse

🔁 Option B: IPv4-Tunnel mit Dienste wie Tunnelbroker.net
Du bekommst dort eine feste IPv6/IPv4-Adresse per Tunnel

Funktioniert, aber ist komplexer und braucht IPv6-fähigen Router

    C:\Users\lbrau>ping -6 lbraun.nl

Ping wird ausgeführt für lbraun.nl [2a02:3102:6894:5200:3e04:6d1d:4553:25c8] mit 32 Bytes Daten:
Zielhost nicht erreichbar.
Zielhost nicht erreichbar.
Zielhost nicht erreichbar.
Zielhost nicht erreichbar.

Ping-Statistik für 2a02:3102:6894:5200:3e04:6d1d:4553:25c8:
    Pakete: Gesendet = 4, Empfangen = 0, Verloren = 4
    (100% Verlust),
